import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'

// PHASE 1: Base44 disabled - no imports or SDK usage

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
